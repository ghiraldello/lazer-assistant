# ===========================================
# EOD Assistant - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── Authentication (GitHub OAuth) ────────────────────────────
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Set the callback URL to: http://localhost:3000/api/auth/callback/github
# (or your production URL)
AUTH_GITHUB_ID=your_github_oauth_client_id
AUTH_GITHUB_SECRET=your_github_oauth_client_secret

# Random secret for session encryption (generate with: openssl rand -base64 32)
AUTH_SECRET=your_random_secret_here

# ── Database - Turso (remote SQLite) ─────────────────────────
# Create a database at: https://turso.tech
# For local dev without Turso, use: file:./dev.db
TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token

# ── Note ─────────────────────────────────────────────────────
# GitHub tokens, Jira credentials, and LLM keys are now managed
# per-user through the Settings page (stored in the database).
# No need to configure them here.
